---
name: niceui
description: 面向网页与应用界面的设计、实现和审查工作流。用于创建或改造页面与组件，处理布局、视觉层级、样式、响应式、交互状态、动效和可访问性，或评估现有界面的体验质量。当用户明确调用 $niceui，或要求设计、实现、优化、重构、审查界面与用户体验时使用。
---

# NiceUI

以用户任务和项目语境为起点，完成可用、连贯且经得起验证的界面。默认保证清楚、稳定和可操作，但不预设安静、中性、杂志感或其他视觉风格；视觉语言必须从产品、用户、内容和参考证据中推导。

## 核心原则

1. 先解决用户任务，再处理视觉表达。
2. 先理解项目已有约定，再决定保留、扩展或调整。修改或重构已有页面时，默认保留现有视觉语言，只在任务要求范围内改变；更换视觉语言必须由用户明确要求。
3. 使用清晰的层级、稳定的布局和一致的交互建立秩序。
4. 让每个视觉或动效选择都有功能、语义或品牌理由。
5. 完成真实可用的状态与流程，不只制作静态理想状态。
6. 使用实际运行结果验证判断，不凭代码外观推测最终体验。
7. 将对齐、可读性、对比度、稳定布局和状态完整性作为跨风格质量底线；字号关系、边界、圆角、材质、色彩面积、留白密度和动效强度不是 NiceUI 的固定审美默认值。
8. 为可交互元素提供带平滑过渡的悬停反馈，并按操作语义设置鼠标指针。
9. 从产品对象、用户动作与真实内容中建立产品指纹，并提炼一个强度适合场景的辨识点；不能用通用落地页骨架代替产品判断，也不能为了独特而破坏可用性。
10. 建立适合当前产品的字体与语义颜色系统，并精确处理水平、垂直、基线和网格对齐。
11. 滚动容器必须使用自定义轨道与滑块视觉；下拉、复选、日期等表单控件默认使用可访问的自定义组件；所有功能图标统一来自选定的图标库。
12. 颜色强度与面积必须有产品、品牌、内容或状态依据。中性、低饱和、强色、单色沉浸和深色聚光都是候选方向，不把其中任何一种设为跨产品默认。
13. Dialog、Drawer、Popover、Dropdown、Tooltip 等浮层必须实现并验证进入与退出过渡，不能只写静态样式或只处理打开瞬间。
14. 在新建或明显重塑页面时，先形成三个实质不同的灰度结构剪影，再根据任务选择布局骨架、间距关系、细节标准和一个产品相关的创意点，然后进入编码。
15. 先判定产品载体与页面职责。博客、官网、后台、商店、作品集、文档和工具不是同一种页面，不得共享同一套默认骨架、信息密度与视觉语气。
16. 交付物必须表现为真实产品，而不是设计讲解稿。页面内不出现"设计说明""批注""示例区域""为什么这样设计"等面向评审者的元叙事；设计依据放在最终回复中，不放进产品界面。
17. 后台、工具、编辑器和高频工作台打开后必须优先呈现可操作对象，不设置营销 Hero、品牌宣言或介绍方法的大标题；官网与内容页面也不能把超大文字当作默认视觉方案。
18. 控制解释文案预算。能通过字段名、数据、状态、内容和操作直接理解的界面不再补充说明段落；帮助文字只用于避免错误、解释不可逆影响或补足必要上下文。
19. 把视觉参考视为偏好证据，不视为待复制页面。提取跨样本成立的关系和细节，根据当前产品重新组合；不保存参考图的固定布局、色值、尺寸或组件外观。
20. 区分正式交付物与验证样本。优化 NiceUI 本身时，先修改 Skill，再用独立页面进行前向验证；测试页面只暴露规则问题，不能成为 NiceUI 的默认模板或产品定义。
21. 全页面任何文字的计算字号不得超过 `22px`。通过字重、行高、留白、位置和颜色建立层级，不得用更大的标题、数字或宣传文案绕过限制。
22. 页面类型只决定问题边界，不决定唯一布局。同一类型必须根据内容关系在多个构图模式中选择，不能把博客永远做成单列文章流，也不能把官网永远做成普通 Hero。
23. 新页面必须与当前项目近期页面执行风格距离检查；不能只更换内容和骨架，却继续复用相同的暖灰底色、等宽眉题、小标题、居中限宽、柔和圆角和轻微悬停等整套视觉语气。**同一批交付或连续产出的多个页面/项目之间同样必须执行风格距离检查**：至少在一个主要维度（色彩模式、色相家族、明暗模式、材质、圆角系统）上形成可解释的差异，不允许把「近白暖灰画布 + 低饱和灰绿/灰蓝品牌色 + 磨砂半透明表面 + 8/12/16 圆角」整套模板复用给每一个交付。
24. 当用户给出“类似某产品/网站/应用”的参考时，必须先抽取参考的内容结构、真实对象、操作入口、信息密度和首屏抓点，再映射到当前产品；不得只借用主题词、色彩或氛围，也不得整页复刻参考。
25. 新建页面的首屏必须在隐藏品牌名后仍能通过真实对象、数据、媒体、状态或操作关系识别产品类型；如果首屏只剩标题、说明、通用按钮、抽象图形或均质卡片，则必须重新推导内容结构。

## 工作流程

### 1. 理解任务

- **判定任务类型**：区分「从零创建」「修改 / 重构已有项目」「仅审查」。
  - 从零创建 → 走完整设计流程（确定方向 → 剪影 → 指纹 → 实现）。
  - 修改 / 重构 → 走保留式流程：读取 [修改与重构已有项目](references/modify-existing.md)，先提取现状、判断改动等级，再在现有视觉语言内做最小改动；不套用「重新设计」流程。
  - 仅审查 → 只输出按严重程度排序的问题与修改方向，不修改代码。
- 明确目标用户、核心任务、使用频率、设备范围和交付边界。
- 同时识别产品载体与页面职责，例如博客首页、官网产品页、后台列表页、商店商品详情或工具编辑器；不能只用"网站""页面"作为类型结论。
- **推导领域内容模型**：在开始设计前，自问并回答：用户来这个网站做什么？这个类型的网站需要哪些核心内容类型才能让用户完成任务？列出 4-8 个核心内容类型作为设计依据。例如音乐平台可能需要歌手/专辑/曲目/播放控制，但具体需要什么取决于子类型（电台、流媒体、乐评、唱片店、音乐人主页各不相同）。未做此推导直接进入编码的，判定为内容缺失。
- 优先从代码、设计系统、相邻页面和现有内容中推断信息。
- 仅在关键选择无法推断且会显著改变结果时询问用户。

### 2. 检查项目

- 找出技术栈、入口、组件库、设计变量、字体、图标和资源处理方式。
- **图标库确认**：如果用户未指定图标库，按技术栈推荐默认方案（Web: Lucide / React: lucide-react / Flutter: Material Icons），并向用户说明可以自行指定（Iconfont、自家 SVG 等）。用户指定的图标库优先级高于默认推荐。
- **检测技术栈并加载对应的实现规则**：
  - 检测到 `*.css` / 无框架 / 原生 HTML → 读取 [Web 实现规则](references/impl-web.md)
  - 检测到 `React` / `Next.js` / `Tailwind` / `styled-components` → 读取 [React 实现规则](references/impl-react.md)
  - 检测到 `Flutter` / `Dart` / `widget` → 读取 [Flutter 实现规则](references/impl-flutter.md)
  - 检测到 `Vue` / `Nuxt` → 同样参考 [React 实现规则](references/impl-react.md)（组件化逻辑通用）
  - 未检测到已知框架 → 默认读取 [Web 实现规则](references/impl-web.md)
- **当任务是修改 / 重构已有页面时**，按 [修改与重构已有项目](references/modify-existing.md) 提取现有视觉语言（设计变量、组件库、排版、间距、圆角阴影、密度、动效），在现有系统内完成改动，不重新推导整套视觉方向。
- 检查相关页面及组件，识别可复用模式和必须保持一致的行为。
- 查看工作区已有改动，保留与当前任务无关的用户工作。

### 3. 确定方向

- **修改 / 重构任务不执行「重新设计」流程**：不生成三个结构剪影、不重新选择气质维度、不重建产品指纹，除非用户明确要求重塑页面。按 [修改与重构已有项目](references/modify-existing.md) 先提取现状、判断改动等级，再在现有视觉语言内做最小改动。
- 根据产品载体、页面职责和使用场景确定导航方式、信息密度、视觉语气、内容证据与交互强度。
- 在类型路由之后读取可用的构图模式，选择与内容关系匹配的模式，而不是直接套用该类型最常见的页面骨架。
- **当产品大类（如"音乐网站""电商平台""社交应用"）有多个常见子类型时，必须先确定子类型**，而不是直接套用该大类最流行的模式。例如"音乐网站"至少包括：流媒体平台、音乐人主页、厂牌官网、音乐杂志、电台、音乐节、唱片店、乐评博客、音乐教育等。每个子类型的布局、密度、配色和交互模式都不同。未做子类型区分直接编码的，判定为方向缺失。
- 当用户给出明确参考，例如“类似某应用、某平台、某网站、某截图”时，先在 [产品辨识与非模板化](references/product-specificity.md) 中完成参考结构抽取，并把抽取结果作为当前任务的高优先级约束；视觉参考只能转译为内容关系和交互关系，不能固化为默认风格。
- 优先形成一个明确方向；只有在比较能解决真实决策时才制作多个方案。
- 读取 [设计决策系统](references/design-decision.md)，形成经过筛选的设计方向。
- 从 [设计方向词典](references/design-direction.md) 中选择 **3-5 个**气质维度，说明为什么适合当前产品，并作为编码的视觉目标。未选择方向直接进入编码的，判定为方向缺失。
- 在编码前读取 [设计方法论](references/design-methodology.md)，按其中步骤依次确定：页面目标 → 页面结构 → 栅格与间距系统 → 字号层级 → 颜色系统 → 组件规则 → 响应式行为。不允许跳过前序步骤直接调颜色或加装饰。
- 在设计或实现界面前读取 [设计基线](references/design-baseline.md)。
- 新建页面、明显调整整体结构、使用视觉参考，或用户认为结果具有模板感、AI 感时，读取 [产品辨识与非模板化](references/product-specificity.md)，建立产品指纹、识别页面类型、生成三个结构剪影并执行模板风险检查。
- 用户提供一张或多张视觉参考时读取 [视觉参考学习](references/reference-learning.md)，区分稳定偏好、局部做法与互相冲突的候选，不整页模仿或平均化。
- 需要主动寻找视觉、真实产品或动效参考，或需要把参考交给 AI 定向生成时，读取 [参考发现与节奏拆解](references/reference-discovery.md)，按页面问题选择来源并形成可实现规格。
- 新建页面或明显调整整体布局时读取 [构图与细节](references/composition-detail.md)，确定空间关系、完成度细节和唯一创意点。
- 为当前项目确定字体与配色时读取 [视觉系统](references/visual-system.md)，根据产品语境生成变量，不套用固定色值、固定色相或预制主题。
- 涉及表单、图标、滚动容器时读取 [设计基线](references/design-baseline.md) 的组件规则。
- 涉及交互、状态变化、浮层或动效时读取 [交互基线](references/interaction-baseline.md)。

### 4. 完整实现

- 遵循项目已有框架、组件和样式组织方式。
- 使用最终用户会看到的真实标题、字段、数据、状态和操作。未知内容使用语义完整、长度可信的拟真内容，不显示 Lorem ipsum、占位符、示例标签或设计过程说明。
- **内容必须包含该产品类型的领域特有元素**。例如：音乐网站必须有播放控制/曲目列表/收听链接；电商必须有商品规格/价格/加入购物车；后台必须有数据/状态/操作入口；博客必须有文章正文/发表日期/分类。只做通用布局（标题+卡片+按钮）而没有领域特有内容的，判定为内容缺失。
- 完成首屏任务密度检查：工具与后台的第一个视口必须允许用户开始核心任务；删除不推动任务的口号、方法介绍、版本宣言和大段辅助文案。
- 完成首屏识别力检查：首屏至少包含一个主要产品对象，并根据页面类型补足若干真实材料，例如内容条目、商品、项目、数据、文件、地图、日程、交易状态、媒体截图、用户动态或操作记录；不得让抽象背景、渐变、假图表或空泛文案承担主要识别。
- 将页面当作已经上线的产品实现：链接应指向合理目标，按钮应有结果，暂不可用的动作应明确禁用，不制作只有外观没有行为的假控件。
- 覆盖正常、悬停、聚焦、按下、禁用、加载、空数据、错误和成功等与任务相关的状态。
- 保证主要工作流可以真实操作，控件语义与操作结果一致。
- 控制改动范围，不借界面任务进行无关重构。
- 根据第 2 步检测到的技术栈，遵循对应的 `impl-*.md` 中的实现规则。

### 5. 验证结果

- 运行项目已有的类型检查、测试、代码检查和构建命令。
- 在浏览器验证前先完成静态视觉门禁：页面类型路由、解释文案、标题面积、最大计算字号、结构手段、卡片重复、主题色面积和相邻页面风格距离必须逐项通过，不能只检查语法和溢出。
- 在浏览器验证前完成首屏 1 秒识别门禁：缩略观察时必须能看出产品类型、主对象和主要动作；如果只能看出“干净网页”“漂亮 Hero”或“通用 Dashboard”，不进入实现验收。
- 在代表性的桌面与移动尺寸下检查真实渲染结果。
- 验证交互、内容溢出、键盘操作、焦点、对比度、动效偏好和资源加载。
- 完成交付前读取并执行 [验证清单](references/verification.md)。

### 6. 迭代 NiceUI

- 当当前任务是改进 NiceUI Skill，而页面仅用于验证时，读取 [Skill 验证协议](references/skill-evaluation.md)。
- 从失败结果提炼可复用的判断或门禁并修改 Skill；不要把时间主要用于修饰验证页面。
- 修改规则后再运行验证样本，记录它通过或暴露了什么；单个样本不能证明规则适用于所有页面类型。

## 决策规则

- 用户明确要求高于本技能的默认基线。
- **修改 / 重构已有项目时，保留现有视觉语言的优先级高于 NiceUI 的默认基线**；只在任务要求范围内改动。用户明确要求重塑时才更换视觉语言。
- **连续产出多个页面或示例时，每个交付必须与上一个交付拉开可观察的风格差异**：至少一个主要维度（色彩模式、色相家族、明暗、材质、圆角系统）不同。无品牌资产时按 [视觉系统](references/visual-system.md) 的产品类型色相表锁定色相，并与最近产出核对，不允许连续复用同一色相家族。
- **批量产出执行批次配额（当一次生成 ≥3 个页面/示例时）**：把整个批次视为一个整体做配色分配，避免同质化堆积：
  - 交付前列出批次内每个交付的品牌色**色相家族**与**色彩模式**，逐一核对占比
  - 任何单一色相家族在批次内的占比 ≤ 1/3（10 个样本中同一家族最多 3 个，出现 6 个同家族即超标，必须重排）
  - 批次内至少覆盖 3 种不同色彩模式（中性主导 / 内容取色 / 深色聚光 / 单色沉浸 / 低色度分类），且浅色与深色交付不能全部只有一种明暗
  - 即使单页配色本身协调、行业上合理，只要批次占比超标就要把多余的同类样本改成其他色相家族
- **用户直接提供主色（如 "用 #635BFF" 或 "品牌色是蓝色"）时，用户主色直接作为 `--brand` 使用，不重新推导、不擅自改色。** 其余色板（hover、弱背景、辅助色）从该主色派生。参考 [视觉系统](references/visual-system.md) 的「用户提供主色时的处理流程」。
- 项目已有设计系统高于通用偏好，除非任务要求重塑该系统。
- 参考案例和流行趋势只能提供候选项，不能覆盖用户偏好、项目语境与本技能规则。
- 选色时可参考 [主题色参考](references/theme-references.md) 中用户认可的颜色，用于校准色感；但参考色不是模板，具体主色仍须从产品内容推导或用用户当前给出的色值。
- 在多个方案都合理时，选择认知负担更低、实现更稳定、后续维护更容易的方案。
- 在视觉效果与可用性冲突时，优先保证信息理解和任务完成。
- 不确定某项装饰或动效是否必要时，先移除，再判断界面是否因此失去信息或反馈。
- 防呆门禁中的数值（22px、150–300ms、≤4 色、面积比例等）是硬性约束，必须遵守，不存在"根据情况调整"的例外。用户明确要求或项目已有设计系统才能覆盖这些数值。

## 防呆门禁（跨平台通用）

以下规则对所有平台通用。不满足这些门禁的输出直接判定为不合格，不进入审美审查环节。

### 硬性门禁（跨平台，无例外）

以下五条不满足即为不合格，不进入后续审查：

1. **全页面任何文字计算字号不得超过 22px**。无例外。通过字重、行高、留白、位置和颜色建立层级。
2. **禁止使用原生表单控件**。`<select>`、复选框、单选框、日期选择器必须使用自定义 accessible 组件实现（Web 需支持键盘导航和表单行为，React 使用 Radix UI / Headless UI 的对应组件，Flutter 使用 Material Widget）。不允许直接暴露浏览器/平台原生控件外观。
3. **禁止使用 CSS 渐变替代真实图片**。所有产品图、人物头像、专辑封面、作品截图、品牌 Logo 等视觉素材必须使用真实图片（通过 `<img>` 标签或 CSS `background-image: url()` 引用外链图片）。不允许用 `linear-gradient`、`radial-gradient` 或纯色色块充当图片内容的占位。在无法获取用户真实图片时，可使用 `picsum.photos`、`unsplash.com` 等免费图源作为演示图片。
4. **禁止无过渡的状态跳变**。所有 hover / focus / active / disabled / 弹出 / 收起 状态变化必须使用平滑过渡（CSS transition 或框架动画库），时长控制在 150–300ms。不允许瞬间显示/隐藏或颜色跳变。
5. **禁止使用 Emoji 替代功能图标**。所有导航、操作、状态、提示等功能性图标必须使用统一图标库的矢量图标（内联 SVG 或框架图标组件）。不允许使用 Emoji 字符（★、♥、●、→ 等）作为界面图标。没有指定图标库时默认使用内联 SVG。

### 基线偏好（跨平台，用户未明确指定时生效）

1. **白天模式优先**：除非用户明确要求深色模式，或产品类型天然适合暗色主题（游戏、影音、夜间工具、创意展示等），否则默认使用浅色/白天主题（`--canvas` 明度 ≥ 85%）
2. **配色极简**：全站配色不超过 4 种颜色（含黑白）：
   - **背景层**：白色或近白色（`--canvas`、`--surface`），不使用有色背景
   - **品牌色**：一个主题色，用于按钮、链接、选中态等主要交互元素
   - **辅助色**：一个辅色，用于标签、徽标、次要强调。辅助色与品牌色为同一色系或邻近色相，不引入冲突色相
   - 品牌色和辅助色的饱和度控制在 30%-60%，仅用于小面积点缀。大面积色块（背景、卡片）保持无彩色
3. **表面色差异**：`--canvas`、`--surface`、`--surface-hover`、`--border` 之间的明度须有可辨识差异，不使用有色背景层

### 用户风格偏好（当用户为 lxhcool 时，以下规则覆盖所有默认基线）

此用户经多次交付验证，确认有以下稳定设计偏好。当用户为 lxhcool 时，这些规则优先级高于所有默认基线和反模式清单中的正向建议：

> 这些偏好定义的是**工艺与质量底线**（克制、精致、分层、无缝），不定义固定的色板与材质组合。即使是为 lxhcool 交付，连续多个页面/示例之间仍必须遵守「跨样例差异」规则：色相家族、色彩模式或明暗必须随产品变化，不能把「磨砂玻璃 + 低饱和灰绿 + 细阴影」当作所有产品的唯一默认样式。

1. **极简用色**：全页面颜色总数 ≤ 4 种（含黑白灰），色相跨度小，不出现冷暖冲突或饱和度跳跃。使用同色系或邻近色相，以明度和饱和度区分层级而非更换色相
2. **磨砂圆角**：卡片、面板、弹窗等容器使用圆角（`border-radius: 8px–16px`），配合 `backdrop-filter: blur()` 或半透明表面营造磨砂玻璃质感
3. **阴影分层**：使用 `box-shadow` 建立层级而非边框或分割线。卡片之间通过阴影深度（1–3 层）区分远近关系，不使用 `border` 或 `hr` 做分割。阴影克制使用：偏移 ≤ 8px，模糊 ≤ 32px，色值使用低色度 `rgba(0,0,0,0.06–0.12)`，不允许纯黑阴影或超大模糊
4. **动画贯穿**：所有状态变化、出现/消失、列表更新必须使用过渡动画（时长 200–300ms）。禁止任何无动画的状态跳变
5. **无边线分割**：内容分组通过留白（间距）、背景差（不同表面色）或阴影实现，不使用边框、分割线或贯穿式横线
6. **小字优先**：全页面最大字号不超过 20px（比技能上限 22px 更严格），正文以 14px 为主，不使用夸张的大标题或超大字重
7. **简洁突出主体**：每页只有一个视觉主体，其他元素降级让位。装饰元素 ≤ 1 个，通过负空间（留白）和对比度（而非添加元素）引导注意力

### 反模式清单（跨平台）

以下组合出现任意一组即为模板化输出，必须拒绝，重新推导方向：

- 近白/暖灰画布 + 低饱和灰绿或灰蓝品牌色 + 磨砂半透明表面（`backdrop-filter`）+ 8/12/16 圆角 + 细阴影 + 小字排版——**「默认克制」模板**；单个页面可以出现，连续多个交付整套复用即判定，必须换色相家族、色彩模式或材质
- 暖灰底色（#F4F1EA / #F5F0E8 等）+ 赤陶/锈色强调色 + 衬线标题 + 居中限宽 + 三卡片行
- 纯黑/近黑底色 + 荧光绿/品红/电蓝强调色 + 等宽眉题 + 大号无衬线标题
- 深色侧栏 + 白色内容区 + 靛蓝强调色 + 圆角卡片 + 右上角头像菜单（后台默认模板）
- 浅色侧栏 + 白色内容区 + 深色顶栏 + 纯白统计卡片 + 左图标右文字的行模式（另一种后台模板）
- 后台页面不得同时出现：左侧导航侧栏 + 白色统计卡片 + 蓝色/靛蓝强调色 + 右上角头像下拉。这是最通用的后台模板，即使换色也是同一种模板
- 居中白色卡片 + 渐变/纯色背景 + Logo 在上 + 两个输入框 + 一个按钮（登录页默认模板）
- 登录页不得在视觉上与登录后的页面完全脱节（不同色系、不同字体、不同圆角策略），登录页必须是整体产品的一部分
- 米白画布 + 贯穿式分割线 + 报章多列 + 零圆角 + 密集正文（需有内容依据）
- 同一页面同时出现：胶囊标签 + 双按钮 CTA + 右侧 Mockup + 三特征卡片 + 01/02/03/04 步骤编号 + 深色 CTA 区段（出现四项以上即判定）

### 密度与尺度管控（防巨型 UI）

模型常将"高级感"错误理解为"放大一切"，产生超大标题、超大卡片、超大留白的低密度页面。以下规则防止尺度膨胀：

1. **卡片使用限制**：卡片只用于表达独立对象（商品、项目、文章预览、数据模块、可点击入口）。普通段落、区块标题、简单列表、两三行说明**不允许使用卡片容器**。能通过排版和留白表达的层级，不使用卡片。连续卡片区块不得超过 1 个，一屏内可见卡片不超过 6 个
2. **信息密度匹配**：根据页面类型选择密度档位，不允许所有页面使用同一密度：
   - 营销/品牌页：中低密度（区块间距 64–80px）
   - 内容/阅读页：中密度（区块间距 48–64px，正文宽度 ≤ 720px）
   - 工具/后台页：中到高密度（区块间距 24–40px）
   - Dashboard/数据页：高密度（区块间距 16–24px）
3. **首屏信息量**：首屏（按 850px 视口高度计算）不得仅显示标题和留白。首屏必须同时包含：导航 + 标题 + 说明 + 主要操作 + **至少部分产品视觉或下一模块入口**。标题不得超过 2 行，说明不得超过 3 行
4. **间距节奏**：间距必须有大小交替，不允许全部间距一致。推荐比例：关联内容 8px → 标题与描述 12–16px → 内容组内部 24px → 组件之间 32px → 区块内部 48px → 区块之间 64–80px。不允许标题与描述之间 32px 然后区块之间也是 32px
5. **内容丰满度**：每个区块必须包含足够的内容项以达到该类型页面的合理信息密度。参考最小值：
   - 网格类区块（歌单、商品、文章、专辑等）：每行 ≥ 4 项，至少 2 行，一屏可见 ≥ 8 项
   - 列表类区块（曲目、订单、通知等）：每屏可见 ≥ 8 行
   - 横向滚动区块（最近播放、推荐等）：可见 + 隐 ≈ 10–16 项
   - 少于上述数量的区块应合并或移除，不允许出现"3 张卡片占一屏"的空洞区块
6. **非 AI 放大自检**：交付前检查是否存在以下 AI 尺度膨胀信号，有则必须缩小：
   - 一个标题占据超过视口高度的 25%
   - 简单卡片内空白面积大于内容面积
   - 一屏内看不到两个有意义的内容组
   - 区块间距反复超过 100px
   - 普通正文内容被展示得像营销标题
   - 卡片仅用于制造视觉重量而无实际内容分组作用

### 平台实现规则

每个平台有自己的机械门禁。根据第 2 步检测到的技术栈，读取对应的实现文件并遵守其中的所有规则：

- **Web（CSS 原生）** → `references/impl-web.md`
- **React / Vue / Tailwind** → `references/impl-react.md`
- **Flutter** → `references/impl-flutter.md`

## 输出要求

- 说明完成了什么，以及重要设计决策如何服务用户任务。
- 对新建或重塑页面说明产品指纹、选定骨架与被拒绝的通用模式；不需要展示未被采用的完整方案，除非比较本身帮助用户决策。
- 设计解释只出现在对用户的交付回复中，不能作为标题、卡片、批注、角标或装饰文案进入最终产品页面，除非产品本身就是设计审查或教学工具。
- 列出实际执行的验证；无法执行的检查必须明确说明。可按 [设计方法论](references/design-methodology.md) 中的 100 分制质量标准自评。
- 审查任务先输出按严重程度排序的问题，并提供具体位置与修改方向。
- 不使用空泛的"更现代""更高级""更有质感"代替可观察的设计理由。必须使用 [设计方向词典](references/design-direction.md) 中的术语描述页面气质。
- 回复末尾必须输出 5 秒自检结果，根据检测到的平台选择对应检查项：
  - **跨平台必检项（所有平台）**：
    ```
    ☐ 全页面最大字号 ≤ 22px
    ☐ 无 lorem ipsum 或占位文案
    ☐ 无 CSS 渐变替代真实图片（使用 picsum/unsplash 等）
    ☐ 无原生表单控件（select/checkbox/date 已自定义）
    ☐ 无 Emoji 替代功能图标（使用 SVG 或图标组件）
    ☐ 所有交互状态有平滑过渡（非瞬间跳变）
    ☐ 已执行风格距离检查
    ☐ 产品指纹明确（非通用骨架）
    ☐ 首屏 1 秒内可识别产品类型和主要对象
    ☐ 白天模式优先（除非用户指定深色或产品类型适合暗色）
    ☐ 无大面积高饱和色块（品牌色 S ≤ 60%，大面积色 S ≤ 15%）
    ☐ 配色不冲突（不超过 2 个色相，品牌色与中性色协调）
    ☐ 无卡片滥用（卡片仅用于独立对象，非所有内容装卡片）
    ☐ 无巨型 UI（首屏有内容非仅标题，间距有节奏非全部一致）
    ☐ 信息密度与页面类型匹配（后台不高密度 ≠ 营销页低密度）
    ☐ 内容丰满度达标（网格≥8项，列表≥8行，横向≥10项）
    ☐ 领域内容模型完整（包含该类型产品的核心内容类型）
    ☐ 列表/卡片内容完整可见（无被遮挡或截断现象）
    ```
  - **平台特定检查项**：从对应的 `impl-*.md` 中复制，格式保持一致。
  - **修改/重构必检项（当任务是修改/重构已有页面时，附加以下项）**：
    ```
    ☐ 保留现有视觉语言（未更换配色/字体/圆角/阴影系统）
    ☐ 仅改动任务相关区域（diff 无无关改动）
    ☐ 与相邻页面风格一致（已执行风格距离检查）
    ☐ 新增元素可从现有 token 派生（无孤立色值/字号/圆角）
    ☐ 原问题已解决（回到任务目标逐项核对，而非"看起来不同了"）
    ```
  缺少自检结果的回复视为未完成验证。
- **批量交付（一次生成 ≥3 个页面/示例）时**，在回复末尾额外输出批次色相分布：列出每个交付的品牌色色相家族与色彩模式，确认无单一家族占比 > 1/3、至少覆盖 3 种色彩模式；未列出批次分布视为未完成批次配色分配。
